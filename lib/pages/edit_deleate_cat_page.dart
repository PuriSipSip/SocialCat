import 'package:flutter/material.dart';

class EditAndDeleteCatPage extends StatelessWidget {
  const EditAndDeleteCatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit And Delete Cat'),
      ),
      body: const Center(
        child: Text('Edit And Delete Cat'),
      ),
    );
  }
}
