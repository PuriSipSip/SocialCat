import 'package:flutter/material.dart';

class UtcccatPage extends StatelessWidget {
  const UtcccatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Utcc Cats'),
      ),
      body: const Center(
        child: Text('Utcccats Page Content'),
      ),
    );
  }
}
